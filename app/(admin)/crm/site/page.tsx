import SiteEditor from "./SiteEditor";

export const metadata = {
  title: "Editor do Site | CRM - Imob Pro",
  description: "Personalize os serviços, mídias e depoimentos exibidos no site público da imobiliária.",
};

export default function CrmSitePage() {
  return <SiteEditor />;
}
